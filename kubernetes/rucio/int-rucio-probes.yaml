probeSettings:  
  ftsProxySecret: cms-ruciod-int-rucio-x509up 

probes:
  check_not_OK_rules:
    interval: 1
  check_expired_rules:
    interval: 1

config:
  monitor:
    prometheus_labels: '{"rucioInstance": "int"}'
