kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: graphite-dev-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30G
  storageClassName: manila-cephfs-share
