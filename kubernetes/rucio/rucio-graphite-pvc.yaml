# eventually should be a separate pvc for each instance
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: graphite-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2G
  storageClassName: manila-cephfs-share
