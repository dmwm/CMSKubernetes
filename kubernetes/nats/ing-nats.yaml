apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ing-nats
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: test-knative-e4l22553w7go-minion-0.cern.ch
    http:
      paths:
      - path: /nats
        backend:
          serviceName: nats-cluster
          servicePort: 4222
