apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-proxy-server.fullname" . }}-test-connection"
  labels:
    {{- include "auth-proxy-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['auth-proxy-server:443']
  restartPolicy: Never
