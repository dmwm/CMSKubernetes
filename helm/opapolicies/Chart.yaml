apiVersion: v3
name: opapolicies
version: 0.0.7

dependencies:
- name: allowedrepos
  version: 0.0.3
  condition: isEnabled.allowedrepos
- name: replicalimits
  version: 0.0.1
  condition: isEnabled.replicalimits
- name: capabilities
  version: 0.0.1
  condition: isEnabled.capabilities
- name: containerlimits
  version: 0.0.1
  condition: isEnabled.containerlimits
- name: containerrequests
  version: 0.0.1
  condition: isEnabled.containerrequests
