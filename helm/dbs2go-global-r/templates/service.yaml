apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbs2go-global-r.fullname" . }}
  labels:
    {{- include "dbs2go-global-r.labels" . | nindent 4 }}
spec:
  selector:
    app: dbs2go-global-r
  ports:
    - port: 9252
      targetPort: 9252
      name: dbs
